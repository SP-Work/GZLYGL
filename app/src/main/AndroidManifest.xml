<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.otitan.gzlygl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 林业基本情况 -->
        <activity android:name=".activity.JbqkActivity" />

        <!-- 人员结构基本情况 -->
        <activity android:name=".activity.RyjgActivity" />

        <!-- 重点工作目标 -->
        <activity android:name=".activity.ZdgzActivity" />

        <!-- 林业资金投入统计 -->
        <activity android:name=".activity.LyzjActivity" />

        <!-- 林业案件统计 -->
        <activity android:name=".activity.LyajActivity" />

        <!-- 林业品牌建设 -->
        <activity android:name=".activity.LyppActivity" />

        <!-- 林业灾害情况 -->
        <activity android:name=".activity.LyzhActivity" />

        <!-- 林业科技情况 -->
        <activity android:name=".activity.LykjActivity" />

        <!-- 党群组织建设 -->
        <activity android:name=".activity.DqzzActivity" />

        <!-- 贵州林业横向对比情况 -->
        <activity android:name=".activity.GzlyActivity" />
    </application>

</manifest>